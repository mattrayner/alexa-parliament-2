"use strict";
exports.__esModule = true;
require("mocha");
describe('PostcodeProcessor', function () {
    context('with complete data', function () {
        it('it creates a complete object');
    });
    context('without a constituency', function () {
        it('it creates a partial object');
    });
});
